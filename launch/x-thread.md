# X / Twitter thread — paste-ready (6 tweets)

Post the morning of PH launch day. Every tweet under 280 chars (counts noted).
Media files listed per tweet — screen-record the live pages for video.

## 1 — Hook (video: pura-liquid-hero.html, 10–15s screen recording) [~200 chars]

AI-generated landing pages don't have to look AI-generated.

This liquid-glass hero is one prompt. The whole page is a single HTML file an
agent built and screenshot-verified.

7 live demos, all open source ↓
https://motion-pages.pages.dev

## 2 — The gallery (media: assets/demo.gif) [~180 chars]

It's a Claude Code skill: foggy 3D worlds, glass product stages, dome
galleries, scroll journeys, springy poster walls.

Every demo has a copy-paste prompt. Paste it, get your own world.

## 3 — The trick (image: `launch/assets/glow-compare.png`) [~260 chars]

Why do AI pages look flat? Missing recipes, not missing model.

Real bloom post-processing dies in headless Chrome. The fix: bake the glow into
radial-gradient sprite textures. Fog == background color makes geometry melt
into atmosphere. It's all in the skill.

## 4 — The loop (media: `launch/assets/boreal-sweep.gif`) [~235 chars]

The part I care about most: the agent checks its own work.

It screenshots desktop/tablet/phone headlessly and iterates. Interactive states
get forcing params — ?p=0.6 pins a scroll journey mid-flight so even animation
is verifiable.

## 5 — Beyond WebGL (video: paperworks-posterwall.html drag interaction) [~170 chars]

Not everything needs Three.js.

This poster wall's spring physics is ~15 lines of CSS transforms driven by drag
lag. The skill covers when NOT to reach for WebGL too.

## 6 — CTA [~150 chars]

All MIT. Single HTML files, no build step.

Repo: https://github.com/jiangyurong609/motion-pages
Demos + prompts: https://motion-pages.pages.dev

Star it if you want more worlds 🌍

## Recording notes

- Tweets 3 & 4 media are ready in `launch/assets/` (glow-compare.png is a real
  render of the same seeded scene both ways; boreal-sweep.gif is 4 labeled
  ?p= shots at ~1fps).
- Tweets 1 & 5 still need screen recordings: 16:9, 1600×900 browser window,
  mouse moving slowly (parallax reads on video). Keep each clip under 20s for
  autoplay.
